{
  "name": "exchangeflow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm run preview"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
